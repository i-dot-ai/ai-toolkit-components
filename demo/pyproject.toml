[project]
name = "ai-toolkit-demo"
version = "0.1.0"
description = "Demo UI for the AI Toolkit Components mcp_datastore application"
requires-python = ">=3.12"
dependencies = [
    "streamlit",
    "pyyaml",
    "mcp",
]

[project.scripts]
demo = "app:main"
